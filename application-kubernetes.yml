spring:
  zipkin:
    enabled: true
    baseUrl: http://tracing-server-service.default.svc.cluster.local:9411
    discoveryClientEnabled: false
    messageTimeout: 10
    sender:
      type: web
eureka:
  instance:
    preferIpAddress: true
