# Settings for the VETS-SERVICE application.
vets:
  cache:
    ttl: 60
    heap-size: 100

# Configuration rules changed in Spring Boot 2.4.x see: https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-Config-Data-Migration-Guide
#spring:
#  profiles:
#    active: production
#  cache:
#    cache-names: vets
